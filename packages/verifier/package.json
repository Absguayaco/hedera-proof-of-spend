{
  "name": "@proof-of-spend/verifier",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Takes a receipt, hashes it, queries the topic, reports match / missing / altered.",
  "license": "MIT",
  "main": "src/index.ts",
  "scripts": {
    "verify": "node src/cli.ts"
  },
  "//": "THIS DEPENDENCY LIST IS THE CLAIM. The verifier depends on nothing of ours: one public Hedera SDK, and node:crypto from the standard library. It does not import @proof-of-spend/anchor, @proof-of-spend/buyer, or any askReceipts client. Adding a workspace dependency here would silently invalidate the central claim of the submission.",
  "dependencies": {
    "@hiero-ledger/sdk": "2.87.0"
  }
}
